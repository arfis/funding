query GetProject($id: ID!) {
    getProject(id: $id) {
        id
        name
        description
        freeAllocation
        investments {
            amount
        }
        # Add other fields as necessary
    }
}
