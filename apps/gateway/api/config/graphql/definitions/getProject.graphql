query GetProject($id: ID!) {
    getProject(id: $id) {
        id
        name
        description
        freeAllocation
        hasPermissionToEdit
        hasPermissionToInvest
        investments {
            amount
        }
        # Add other fields as necessary
    }
}
