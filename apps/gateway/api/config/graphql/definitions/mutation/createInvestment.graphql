mutation CreateInvestment($projectId: ID!, $amount: Int!, $ethAddress: String!, $precision: Int!, $status: String!, $txHash: String!) {
    createInvestment(projectId: $projectId, amount: $amount, ethAddress: $ethAddress, precision: $precision, status: $status, txHash: $txHash) {
        id
        amount
    }
}
